export function Viewer(props) {

    return (
        <textarea>
            className="panel"
            rows={35}
            cols={80}
            readOnly="true"
        </textarea>
    );
}
